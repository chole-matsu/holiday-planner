import test from 'node:test';
import assert from 'node:assert/strict';
import {putActivity,parseState,validDate,parseItems,checklistForDay,resizeActivity,maxDuration,eventAt,timeLabel,durationLabel} from '../dist/model.js';
const event=(categoryId,duration=1)=>({categoryId,duration});
test('30分の予定を繰り返し配置し、入力は変更しない',()=>{const before={18:event('reading')};assert.deepEqual(putActivity(before,'reading',19),{18:event('reading'),19:event('reading')});assert.deepEqual(before,{18:event('reading')});});
test('2時間の予定を30分刻みで移動し長さを保持',()=>assert.deepEqual(putActivity({18:event('reading',4)},'reading',25,18),{25:event('reading',4)}));
test('同じ長さの予定は入れ替えできる',()=>assert.deepEqual(putActivity({18:event('reading',4),24:event('manga',4)},'reading',24,18),{18:event('manga',4),24:event('reading',4)}));
test('カテゴリ一覧から同じ長さの予定へ置くと置き換え',()=>assert.deepEqual(putActivity({18:event('reading')},'walk',18),{18:event('walk')}));
test('違う長さの予定や予定の途中への配置は重複を拒否',()=>{const before={18:event('reading',4),24:event('walk',2)};assert.throws(()=>putActivity(before,'manga',19));assert.throws(()=>putActivity(before,'reading',24,18));assert.deepEqual(before,{18:event('reading',4),24:event('walk',2)});});
test('移動元自身の時間に一部重なる移動は可能',()=>assert.deepEqual(putActivity({18:event('reading',4)},'reading',19,18),{19:event('reading',4)}));
test('23:30への配置は可能、24:00を超える移動は拒否',()=>{assert.deepEqual(putActivity({},'sleep',47),{47:event('sleep')});assert.throws(()=>putActivity({18:event('reading',4)},'reading',46,18));for(const start of [-1,48,1.5,NaN])assert.throws(()=>putActivity({},'reading',start));});
test('30分の予定を2時間へ伸ばし再び30分へ縮める',()=>{const before={18:event('reading')};const extended=resizeActivity(before,18,4);assert.deepEqual(extended,{18:event('reading',4)});assert.deepEqual(resizeActivity(extended,18,1),before);assert.deepEqual(before,{18:event('reading')});});
test('延長は次の予定開始まで、他の予定や翌日には侵入しない',()=>{const day={18:event('reading'),22:event('walk')};assert.equal(maxDuration(day,18),4);assert.equal(resizeActivity(day,18,4)[18].duration,4);assert.throws(()=>resizeActivity(day,18,5));assert.equal(maxDuration({47:event('sleep')},47),1);assert.throws(()=>resizeActivity({47:event('sleep')},47,2));for(const duration of [0,-1,1.5,NaN])assert.throws(()=>resizeActivity(day,18,duration));});
test('予定の途中も同じブロックとして判定、終了時刻は含めない',()=>{const day={18:event('reading',4)};assert.equal(eventAt(day,21)[0],'18');assert.equal(eventAt(day,22),undefined);});
test('30分・2時間・24:00の表記',()=>{assert.equal(timeLabel(19),'09:30');assert.equal(timeLabel(48),'24:00');assert.equal(durationLabel(1),'30分');assert.equal(durationLabel(3),'1時間30分');assert.equal(durationLabel(4),'2時間');});
test('旧データは時刻・1時間の長さ・持ち物・チェックを保持して移行',()=>{const original={version:1,categories:[{id:'reading',name:'読書',icon:'📖',color:'sage',items:['本']}],days:{'2026-09-21':{9:'reading',23:'reading'}},checkedByDate:{'2026-09-21':['本']}};const restored=parseState(JSON.stringify(original));assert.equal(restored.version,2);assert.deepEqual(restored.days,{'2026-09-21':{18:event('reading',2),46:event('reading',2)}});assert.deepEqual(restored.categories,original.categories);assert.deepEqual(restored.checkedByDate,original.checkedByDate);});
test('初期版のチェック情報がないデータも移行',()=>{const original={version:1,categories:parseState(null).categories,days:{}};assert.deepEqual(parseState(JSON.stringify(original)).checkedByDate,{});});
test('新形式で長さ・日付・カスタムカテゴリを保存復元',()=>{const state=parseState(null);state.categories.push({id:'custom-test',name:'映画',icon:'✨',color:'blue'});state.days={'2026-09-21':{0:event('sleep',12),19:event('custom-test',4)}};assert.deepEqual(parseState(JSON.stringify(state)),state);});
test('重複、日跨ぎ、不正な長さ、旧形式の不正値を拒否',()=>{for(const day of [{18:event('reading',4),19:event('walk')},{47:event('reading',2)},{18:event('reading',0)},{18:event('unknown')},{48:event('reading')},[]]){const state=parseState(null);state.days={'2026-09-21':day};assert.throws(()=>parseState(JSON.stringify(state)));}const old={...parseState(null),version:1,days:{'2026-09-21':{24:'reading'}}};assert.throws(()=>parseState(JSON.stringify(old)));assert.throws(()=>parseState('{'));});
test('存在しない日付と不正な形式を拒否',()=>{assert.equal(validDate('2028-02-29'),true);for(const date of ['2026-02-29','2026-13-01','2026-9-21','oops'])assert.equal(validDate(date),false);});
test('持ち物は空行・前後の空白・同名の重複を除く',()=>assert.deepEqual(parseItems(' 本 \r\n\n飲み物\n本\n'),['本','飲み物']));
test('長い予定も持ち物は一度だけ集計、縮小・移動後も保持',()=>{const categories=[{id:'reading',name:'読書',items:['本','飲み物']},{id:'walk',name:'散歩',items:['飲み物','帽子']}];const day={18:event('reading',4),24:event('reading'),26:event('walk',2)};const expected=[{name:'本',categories:['読書'],checked:false},{name:'飲み物',categories:['読書','散歩'],checked:true},{name:'帽子',categories:['散歩'],checked:false}];assert.deepEqual(checklistForDay(categories,day,['飲み物']),expected);assert.deepEqual(checklistForDay(categories,resizeActivity(day,18,1),['飲み物']),expected);assert.deepEqual(checklistForDay(categories,putActivity(day,'reading',19,18),['飲み物']),expected);assert.deepEqual(checklistForDay(categories,{}),[]);});
test('チェックは日付ごとに独立して保存復元',()=>{const state=parseState(null);state.categories[0].items=['本'];state.days={'2026-09-21':{19:event('reading',4)},'2026-09-22':{18:event('reading')}};state.checkedByDate={'2026-09-21':['本']};const restored=parseState(JSON.stringify(state));assert.deepEqual(restored,state);assert.equal(checklistForDay(restored.categories,restored.days['2026-09-21'],restored.checkedByDate['2026-09-21'])[0].checked,true);assert.equal(checklistForDay(restored.categories,restored.days['2026-09-22'],restored.checkedByDate['2026-09-22'])[0].checked,false);});
test('不正な持ち物やチェックを拒否',()=>{const state=parseState(null);state.categories[0].items=[null];assert.throws(()=>parseState(JSON.stringify(state)));delete state.categories[0].items;state.checkedByDate={'2026-09-22':{book:true}};assert.throws(()=>parseState(JSON.stringify(state)));});
